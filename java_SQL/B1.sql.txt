1.  SELECT * FROM scores WHERE diemToan = (SELECT MAX(diemToan) FROM scores);
2.  SELECT * FROM scores WHERE diemVan = (SELECT MIN(diemVan) FROM scores);
3.  SELECT * FROM scores WHERE diemVan = (SELECT MIN(diemVan) FROM scores) 
OR diemToan = (SELECT MIN(diemToan) FROM scores) 
OR diemAnh = (SELECT MIN(diemAnh) FROM scores) ;
