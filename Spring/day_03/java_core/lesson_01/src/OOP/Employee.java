package OOP;

public class Employee extends Person{

    public Employee(){
        System.out.println(name);
        System.out.println("Đay là công nhân");  // khi khởi tạo đối tượng class con thì mặc định gọi đến thằng cha trước.
        // rồi mới đến thằng con

        // override dùng trong kế thừa, cha như nào con như thế
        // overload: trong 1 class có 2 hay nhiều hàm giống tên nhưng khác tham số truyền vào ( kiểu dữ lieuj hoạc số lượng truyền vào)
        // đa hình:

    }
}
